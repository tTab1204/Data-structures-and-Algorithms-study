// 속도가 퀵 정렬과 비슷하게 매우 빠르다.
// 최악의 경우에도 퀵 정렬처럼 O(N^2)까지 떨어지지는 않는다.
// 최악의 경우에도 O(N\times log_2{N})의 속도를 가짐. 안정적.

const merge = (left, right) => {
    let result = []
    let leftIndex = 0;
    let rightIndex = 0;

    while(leftIndex < left.length && rightIndex < right.length) {

        if(start < end)

    }

}
// 아직 재귀호출 부분이 이해가 잘 안된다.
// 내일 더 고민해서 공부해보자.