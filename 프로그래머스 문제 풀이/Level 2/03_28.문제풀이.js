// 1. 캐시

// LRU 알고리즘: 가장 오랫동안 참조되지 않은 페이지를 교체하는 기법 (운영체제)
// Linked List를 이용한 queue로 이루어진다.
// LRU 알고리즘 이해, 구현하기

// cache hit: CPU가 참조하고자 하는 메모리가 캐시에 존재하고 있을 경우, Cache Hit이라고 한다.
// cache miss: CPU가 참조하고자 하는 메모리가 캐시에 존재하지 않을 떄는 Cache Miss라고 함.

function solution(cacheSize, cities) {
  var answer = 0;
  return answer;
}
